# PrivacyScrub — Dependencies
# Self-hosted privacy removal platform

# Web framework
Flask==3.1.1
Werkzeug==3.1.3

# HTTP client for scanning & API calls
requests==2.32.3

# HTML parsing for broker scanning
beautifulsoup4==4.13.4
lxml==5.4.0

# Scheduled scanning
APScheduler==3.11.0

# PDF report generation
reportlab==4.4.0

# Date/time utilities
python-dateutil==2.9.0

# Rate limiting
ratelimit==2.2.1

# Fake user-agent rotation
fake-useragent==2.0.3

# SOCKS proxy support (optional)
PySocks==1.7.1

# CSV export (stdlib, listed for clarity)
# csv — included in Python stdlib

# JSON handling (stdlib)
# json — included in Python stdlib

# Hashing (stdlib)
# hashlib — included in Python stdlib

# Threading (stdlib)
# threading — included in Python stdlib
